# --- user data: never commit (see docs/vision.md §5 — Non-negotiables) ---
workspace/
.dev-workspaces/
# legacy dev-sandbox path, superseded by .dev-workspaces/ above
workspaces/
# legacy root-level notes.md, superseded by workspace/notes.md +
# data/account_identity.json (docs/next-phase-plan.md §4) — still ignored
# since real installs may still have one on disk until migrated
/notes.md
results/
data/
.env

# --- python ---
.venv/
__pycache__/
*.pyc
.pytest_cache/
.ruff_cache/
logs/

# --- macOS ---
.DS_Store
